apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "stackchart.fullname" . }}-test-connection"
  labels:
    {{- include "stackchart.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "stackchart.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
