apiVersion: v1
kind: Service
metadata:
  name: {{ include "stackchart.fullname" . }}
  labels:
    {{- include "stackchart.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "stackchart.selectorLabels" . | nindent 4 }}
